using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossProgress : MonoBehaviour
{
    public static bool IceFrozen;
    public static bool IceDone;
    public static bool FireDone;
    public static bool WaterDone;
    public static bool EarthDone;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
